services:
  db:
    image: postgres:16
    container_name: astra_db
    environment:
      POSTGRES_USER: astra
      POSTGRES_PASSWORD: astra
      POSTGRES_DB: astra
    ports:
      - "5432:5432"
    volumes:
      - astra_pgdata:/var/lib/postgresql/data

volumes:
  astra_pgdata:
